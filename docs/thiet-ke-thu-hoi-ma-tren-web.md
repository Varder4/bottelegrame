# BẢN CHỐT — THU HỒI MÃ TRÊN PANEL WEB\n\nMọi đường dẫn gốc `c:/Users/AdminPC/Documents/BOT_Telegram/televip/`. Mọi số dòng đọc từ tệp thật.\n\n---\n\n## 0. Ba quyết định nền, và ba thứ phải được DUYỆT trước khi viết mã\n\nBản này khác thiết kế ban đầu ở ba chỗ gốc rễ; mọi thay đổi còn lại là hệ quả.\n\n| # | Đổi | Vì sao |\n|---|---|---|\n| **N1** | Hàm ghi hàng loạt nhận **`DeNghiThuHoi`** (kiểu chỉ sinh ra sau một `GETDEL` thành công), không nhận `PhamViThuHoi` + `so_ma_da_hien: int` | B.3. `LoNapKho` có **một** nơi dựng (`services/stock.py:210-270`); `PhamViThuHoi` có **hai** — nên chữ ký cũ không phân biệt được "đã có người bấm xác nhận" với "vừa đề nghị xong". Và `so_ma_da_hien` là con số duy nhất trong database ghi lại *con người đã duyệt cái gì*; nó không được là tham số tuỳ ý. |\n| **N2** | Hàng rào tiền lượt cuối đo trên **chính tập `RETURNING`**, không đo bằng một câu `count(*)` đứng trước `UPDATE` | B.6 + TIEN.3. `db/engine.py:66-78` không đặt `isolation_level` ⇒ READ COMMITTED ⇒ hai câu lệnh là hai snapshot. Thứ duy nhất ràng buộc được một `UPDATE` là vị từ nằm trong chính nó, hoặc một phép đo trên đúng cái nó vừa sửa. |\n| **N3** | Vé neo theo **CHỦ SỞ HỮU** (`dac:chu:<actor_id>`), không chỉ theo token | B.2 + TIEN.2. `_issue_ticket` (`apps/worker/handlers/admin/codes.py:406-429`) không xoá vé cũ của chính người đó. `services/broadcast.py:695-697` đã viết thành chữ đúng bất biến này ("Mỗi người tối đa MỘT nháp sống") và thi hành ở `:708-718`. |\n\n**Ba thứ phải xin duyệt (CLAUDE.md §1, §3) — chúng ĐỔI hành vi hiện có, dù không làm đỏ bài kiểm nào:**\n\n1. **Trần tích luỹ theo người cho `/del_code`** (mục A.3.2). Admin gõ `/del_code` lần thứ N trong một giờ sẽ bị từ chối. Đây là vá cho B.4 / QUYEN.1 — lỗ duy nhất **không giới hạn tiền** trong cả bộ soát.\n2. **Một migration chỉ thêm index**: `CREATE INDEX ix_audit_del_code ON audit_log (actor_id, created_at) WHERE action = 'del_code'`. Không có nó thì hàng rào ở (1) là một lần quét toàn `audit_log` trên mỗi lượt `/del_code`. Thêm index, không đổi cột, không đổi hợp đồng sổ.\n3. **Ghi `<lệnh>.denied` cho đường web** trong `apps/adminweb/deps.py` (mục B.4). Chạm **mọi** route web, không riêng thu hồi.\n\nNếu (1) hoặc (3) không được duyệt, mục F phải ghi thẳng rằng lỗ đó **còn nguyên và được chấp nhận có ý thức** — không được viết rằng rào 0 đã chặn nó.\n\n---\n\n## A. DANH SÁCH HÀM SERVICE\n\nTất cả trong **`src/televip/services/code_issuance.py`**, mở rộng khối `── Thu hồi ──` (dòng 415-473). `revoke_one` (`:448`) và `revoke_bulk` (`:460`) **giữ nguyên chữ ký** — chúng là nguyên thuỷ SQL. Nhập `DUAL_APPROVAL_KEY`, `DEFAULT_DUAL_APPROVAL_VND`, `doc_menh_gia` từ `services/stock.py:136-137, 163`; không vòng import vì `stock.nap()` import `code_issuance` **bên trong thân hàm** (`stock.py:314`).\n\n### A.1 Hằng và ngoại lệ\n\n```python\n#: Sentinel TƯỜNG MINH cho "mọi mệnh giá". Chuỗi rỗng KHÔNG bao giờ mang nghĩa này.\nMENH_GIA_TAT_CA: Final = "__tatca__"\n\nDE_NGHI_PREFIX: Final     = "dac:"   # giữ nguyên giá trị codes.py:372\nDE_NGHI_TTL_SECONDS: Final = 300     # giữ nguyên codes.py:376\nCUA_SO_TICH_LUY_GIAY: Final = 3600   # cửa sổ trượt của trần tích luỹ /del_code\n\nclass ThuHoiBiTuChoi(ValueError):\n    """Không thu hồi được. Mọi lớp con mang FIELD có kiểu; tầng trình bày tự viết câu chữ\n    (khuôn `stock.NapKhoBiTuChoi`, stock.py:142-160)."""\n\n# — một mã —\nclass NhieuHonMotMa(ThuHoiBiTuChoi):   def __init__(self, *, so_token: int) -> None: ...\nclass KhongTimThayMa(ThuHoiBiTuChoi):  def __init__(self, *, code_value: str) -> None: ...\nclass MaDaPhat(ThuHoiBiTuChoi):\n    def __init__(self, *, code_value: str, code_type: str, value_vnd: int, holder_id: int,\n                 holder_username: str | None, holder_name: str | None,\n                 grant_state: str, moc: datetime | None) -> None: ...\nclass MaDaThuHoi(ThuHoiBiTuChoi):      def __init__(self, *, code_value: str) -> None: ...\nclass MatCuocDua(ThuHoiBiTuChoi):      def __init__(self, *, code_value: str) -> None: ...\nclass VuotNguongThuHoiTichLuy(ThuHoiBiTuChoi):\n    def __init__(self, *, da_thu_vnd: int, them_vnd: int, threshold_vnd: int,\n                 cua_so_giay: int) -> None: ...\n\n# — hàng loạt —\nclass LoaiKhongHopLe(ThuHoiBiTuChoi):        def __init__(self, *, code_type_tho: str) -> None: ...\nclass MenhGiaKhongDocDuoc(ThuHoiBiTuChoi):   def __init__(self, *, menh_gia_tho: str) -> None: ...\nclass PhamViRong(ThuHoiBiTuChoi):            def __init__(self, *, code_type: str, value_vnd: int) -> None: ...\nclass VuotNguongThuHoi(ThuHoiBiTuChoi):\n    def __init__(self, *, tong_vnd: int, threshold_vnd: int, so_ma: int) -> None: ...\nclass PhamViDaLonLen(ThuHoiBiTuChoi):\n    """Cái sắp bị xoá LỚN HƠN cái con người đã nhìn thấy — về SỐ MÃ hoặc về TIỀN."""\n    def __init__(self, *, so_ma_da_hien: int, so_ma_thuc: int,\n                 tong_vnd_da_hien: int, tong_vnd_thuc: int) -> None: ...\n\n# — vé —\nclass DeNghiHetHan(ThuHoiBiTuChoi): ...       # hết TTL, đã bấm, hoặc bị đề nghị mới ghi đè\nclass DeNghiCuaNguoiKhac(ThuHoiBiTuChoi): ...  # có thật, nhưng không phải của người này\n```\n\n`VuotNguongThuHoi` **không** kế thừa `stock.VuotNguongDuyetHaiNguoi` (`stock.py:146`): cái đó là con của `NapKhoBiTuChoi`, và một route thu hồi bắt `NapKhoBiTuChoi` là câu lệnh vô nghĩa. Dùng chung **hằng số khoá**, không dùng chung cây ngoại lệ.\n\n### A.2 Kiểu dữ liệu\n\n```python\n@dataclass(frozen=True, slots=True)\nclass MaChoThuHoi:\n    """Một mã đã qua đủ hàng rào ĐỌC. CHỈ `kiem_ma_thu_hoi()` dựng được — đúng một nơi\n    dựng, giống `LoNapKho` (stock.py:197-208)."""\n    code_id: int\n    code_value: str        # ← lấy từ DÒNG DỮ LIỆU, không từ tham số. Xem A.3.1.\n    code_type: str\n    value_vnd: int\n    status: str            # luôn 'available' tại đây — giữ để ghi audit.before\n\n@dataclass(frozen=True, slots=True)\nclass PhamViThuHoi:\n    """Phạm vi đã qua đủ hàng rào ở bước ĐỀ NGHỊ. Dùng để DỰNG TRANG XÁC NHẬN và để\n    nạp vào vé. KHÔNG còn là vé vào cửa của hàm ghi (N1)."""\n    code_type: str\n    value_vnd: int         # 0 = không lọc mệnh giá (giữ quy ước codes.py:496-498)\n    so_ma: int\n    tong_vnd: int\n    threshold_vnd: int\n\n@dataclass(frozen=True, slots=True)\nclass DeNghiThuHoi:\n    """Nội dung một vé ĐÃ TIÊU. Chỉ `nhan_de_nghi_thu_hoi()` dựng được, và hàm đó chỉ\n    dựng được sau một `GETDEL` thành công.\n\n    Mang HAI con số 'đã hiện' — số mã VÀ tổng tiền. Đây là chỗ sửa lý lẽ cũ ("không lưu\n    tong_vnd vì mọi con số tiền được tính lại"): có hai loại số khác nhau, và thiết kế cũ\n    gộp làm một.\n      · số QUYẾT ĐỊNH  → phải đếm lại (đúng)\n      · số NGƯỜI TA ĐÃ NHÌN → phải là chính nó, đếm lại là mất nó (TIEN mục 1)\n    """\n    actor_id: int\n    code_type: str\n    value_vnd: int\n    so_ma_da_hien: int\n    tong_vnd_da_hien: int\n\n@dataclass(frozen=True, slots=True)\nclass KetQuaThuHoiHangLoat:\n    so_ma_da_xoa: int      # từ RETURNING, KHÔNG phải số đã hiện\n    tong_vnd: int          # từ RETURNING\n    so_ma_da_hien: int\n    tong_vnd_da_hien: int\n    @property\n    def lech(self) -> bool:\n        return (self.so_ma_da_xoa != self.so_ma_da_hien\n                or self.tong_vnd != self.tong_vnd_da_hien)\n```\n\n### A.3 Hàm — một mã\n\n#### A.3.1 `kiem_ma_thu_hoi`\n\n```python\nasync def kiem_ma_thu_hoi(db: AsyncSession, *, code_value: str) -> MaChoThuHoi:\n    """Bốn hàng rào ĐỌC, đúng thứ tự bot đang chạy (codes.py:775-800).\n\n    0. `token = code_value.split()`; `len(token) != 1` → NhieuHonMotMa\n    1. `_SQL_FIND_CODE` với tham số `token[0]` + `.one_or_none()` → KhongTimThayMa\n    2. `grant_id IS NOT NULL`, bất kể grant.state              → MaDaPhat\n    3. `status == 'revoked'`                                    → MaDaThuHoi\n\n    HAI ĐIỀU BẮT BUỘC, cả hai là vá cho TIEN mục 4:\n\n    a) Truy vấn bằng ĐÚNG cái token đã đếm — `token[0]`, không phải `code_value` thô.\n       `<input>` gửi "ABC123\\r\\n" cho 1 token nhưng `WHERE code_value = 'ABC123\\r\\n'`\n       không khớp dòng nào ⇒ "Không tìm thấy code" cho một mã CÓ THẬT. Hỏng theo hướng\n       đóng, nhưng đó đúng là câu trả lời sai đẩy người vận hành quay ra gõ tay\n       /del_all_code với phạm vi rộng hơn — đường hỏng tệ nhất của cả màn này.\n\n    b) `_SQL_FIND_CODE` phải THÊM `c.code_value` vào danh sách SELECT (bản hiện tại,\n       codes.py:725-740, KHÔNG có cột này), và `MaChoThuHoi.code_value` lấy từ dòng dữ\n       liệu. Nếu không, chuỗi đi vào `audit_log` đến từ Ô NHẬP chứ không từ database.\n\n    KHÔNG `.lower()`: `uq_codes_value` unique trên chuỗi nguyên bản\n    (db/models/codes.py:140), mã phân biệt hoa thường (stock.py:180-186).\n    `.one_or_none()` chứ không `.first()`: nhiều dòng là dữ liệu hỏng và phải NỔ.\n    """\n```\n\n#### A.3.2 `thu_hoi_mot`\n\n```python\nasync def thu_hoi_mot(db: AsyncSession, ma: MaChoThuHoi, *, actor_id: int,\n                      ip: str | None = None) -> int:\n    """Ghi. Gọi trong `transaction()` của nơi gọi. Trả `code_id`.\n\n    Nhận `MaChoThuHoi`, không nhận `code_id: int` — chặn bằng KIỂU (khuôn\n    `nap(db, lo: LoNapKho)`, stock.py:293-304).\n\n    BỐN việc, đúng thứ tự:\n\n    1. `await admin_service.require_permission(db, actor_id, "/del_code")`\n       ── vá QUYEN mục 2. Trần TIỀN đã được đưa vào điểm nghẽn; QUYỀN thì chưa. Ranh\n       giới /del_code (owner+admin) ↔ /del_all_code (owner) hiện chỉ tồn tại ở hai dòng\n       `Depends` và hai decorator. `require_permission` có docstring viết đúng cho chỗ\n       này — "Bản dùng cho chỗ gọi NGOÀI handler Telegram (script, tầng web)"\n       (services/admin.py:184-189) — và thiết kế cũ không dùng nó ở đâu cả.\n       Không thêm phụ thuộc: hàm này đã phải import `services.admin` để gọi `write_audit`.\n       Đường bot đã qua @admin_command nên lượt kiểm này LUÔN đậu; nó là dây bẫy.\n\n    2. TRẦN TÍCH LUỸ theo NGƯỜI, cửa sổ trượt `CUA_SO_TICH_LUY_GIAY`\n       ── vá B.4 / QUYEN mục 1, lỗ KHÔNG GIỚI HẠN TIỀN duy nhất trong bộ soát.\n       `_SQL_TONG_DA_THU_HOI` (A.5) cộng `value_vnd` của mọi mã người này đã thu hồi\n       trong cửa sổ, đọc từ `audit_log ⋈ codes`. Cộng `ma.value_vnd` vào; vượt\n       `settings_service.get_int(DUAL_APPROVAL_KEY, DEFAULT_DUAL_APPROVAL_VND, db=db)`\n       → VuotNguongThuHoiTichLuy.\n\n       "Rào 0 chặn lệnh hàng loạt không ngưỡng" là mệnh đề SAI về cái nó bảo vệ: rào 0\n       chặn MỘT request mang nhiều mã, nó không chạm được tới NHIỀU request mỗi cái một\n       mã, và vòng lặp nằm ở phía người gọi — ngoài tầm của mọi kiểu dữ liệu.\n       Trên bot, thứ giữ con số thấp không phải hàng rào mà là GIÁ của một lượt: một tin\n       nhắn mỗi mã, hiện nguyên văn trong nhóm admin, cộng HAI dòng audit\n       (admin.py:405-414 + handler). Web hạ giá đó xuống gần bằng không: 500 POST trong\n       5 giây, hoàn toàn im lặng, một dòng audit mỗi lượt. Vai trò `admin` — người mà\n       0003_seed_admin.py:54 CỐ Ý từ chối /del_all_code — đọc 20 code_value mỗi lượt\n       `/codes` (codes.py:271-277, LIST_LIMIT=20, mutates=False nên KHÔNG sinh audit)\n       rồi lặp `fetch`. Kho 500 mã 88K = 44.000.000đ.\n\n       Dùng lại `stock.DUAL_APPROVAL_KEY` — không hằng số mới, không khoá mới (khoá chưa\n       seed thì `settings_service.set()` từ chối, tức nút vặn sẽ không vặn được).\n       Nghĩa khớp nguyên văn nhãn đã seed: "Tổng giá trị … vượt mức này thì cần hai người\n       duyệt" (0002_seed_config.py:348-355).\n\n    3. `revoke_one()`; trả None → MatCuocDua. Audit KHÔNG được ghi.\n    4. `write_audit()` — cùng giao dịch (admin.py:302-309).\n    """\n```\n\n### A.4 Hàm — hàng loạt\n\n```python\nasync def kiem_pham_vi_thu_hoi(db: AsyncSession, *, code_type_tho: str,\n                               menh_gia_tho: str) -> PhamViThuHoi:\n    """Bốn hàng rào bước ĐỀ NGHỊ, đúng thứ tự codes.py:489 → 524.\n\n    `menh_gia_tho` là `str`, KHÔNG phải `str | None` — vá TIEN mục 5:\n      · `menh_gia_tho == MENH_GIA_TAT_CA` → value_vnd = 0 (không lọc)\n      · chuỗi rỗng / khoảng trắng          → MenhGiaKhongDocDuoc\n      · còn lại → `doc_menh_gia()`; None   → MenhGiaKhongDocDuoc\n    Sự khác nhau giữa "một mệnh giá" và "TẤT CẢ" không bao giờ được là sự khác nhau giữa\n    có chữ và không có chữ. `Form()` của FastAPI đưa `""` chứ không đưa `None`, nên\n    `menh_gia or None` biến "ô bị bfcache khôi phục về rỗng" thành phạm vi rộng nhất có\n    thể — đạt được bằng cách KHÔNG LÀM GÌ.\n    Bot ánh xạ ở tầng trình bày: `args[1] if len(args) > 1 else MENH_GIA_TAT_CA`.\n\n    Ném: LoaiKhongHopLe, MenhGiaKhongDocDuoc, PhamViRong, VuotNguongThuHoi.\n    """\n\n\nasync def tao_de_nghi_thu_hoi(db: AsyncSession, *, actor_id: int, pham_vi: PhamViThuHoi,\n                              ip: str | None = None) -> str:\n    """Phát vé. Gọi trong `transaction()`. Trả chuỗi vé.\n\n    BỐN việc, đúng thứ tự:\n\n    1. `require_permission(db, actor_id, "/del_all_code")` — QUYEN mục 2. Phát vé là\n       thứ CẤP PHÉP cho lệnh ghi, nên nó phải hỏi cùng câu hỏi.\n\n    2. `write_audit(action="del_all_code.denghi", entity_type="codes",\n                    entity_id=f"{code_type}:{value_vnd or 'all'}",\n                    after={"so_ma", "tong_vnd", "ip"})`\n       ── vá B.5. Trên Telegram mỗi lần gõ /del_all_code sinh một dòng\n       `action="/del_all_code"` do decorator (admin.py:405-414). Trên web, đúc 10 đề nghị\n       để lại CON SỐ KHÔNG. Khi B.1 xảy ra, câu hỏi "đề nghị mở lúc mấy giờ, màn hình nói\n       bao nhiêu mã" chỉ trả lời được bằng structlog — mà admin.py:302-309 chép nguyên\n       bài học về việc dựa vào một nguồn ghi tách rời (`admin_logs` còn 283 dòng cho toàn\n       bộ vòng đời hệ cũ). Hậu tố `.denghi` không lọt vào phép đếm nào: mọi bài kiểm hiện\n       có so BẰNG ĐÚNG chuỗi (test_admin_codes.py:729), và ô lọc Nhật ký cũng vậy\n       (admin.py:502).\n       Ghi audit TRƯỚC khi chạm Redis: hỏng ở Redis thì giao dịch cuộn lại, không còn vé\n       cũng không còn dòng sổ. Ngược lại thì có vé sống mà sổ trắng.\n\n    3. MỘT ĐỀ NGHỊ SỐNG MỖI NGƯỜI (N3):\n           ve = secrets.token_urlsafe(8)\n           ve_cu = await r.set(f"{DE_NGHI_PREFIX}chu:{actor_id}", ve,\n                               ex=DE_NGHI_TTL_SECONDS, get=True)   # SET…GET, nguyên tử\n           if ve_cu:\n               await r.delete(f"{DE_NGHI_PREFIX}ve:{ve_cu.decode()}")  # đốt vé cũ CỦA\n                                                                      # CHÍNH người này\n       KHÔNG đụng vé của người khác — giữ luật codes.py:607-616. Redis 6.2+ đã là điều\n       kiện sẵn có (`getdel` dùng ở codes.py:434).\n\n    4. `SET {DE_NGHI_PREFIX}ve:{ve}` = JSON {actor_id, code_type, value_vnd, so_ma,\n       tong_vnd}, `ex=DE_NGHI_TTL_SECONDS`.\n       CÓ lưu `tong_vnd` — xem `DeNghiThuHoi`.\n    """\n\n\nasync def nhan_de_nghi_thu_hoi(*, ve: str, actor_id: int) -> DeNghiThuHoi:\n    """Tiêu vé. KHÔNG chạm DB.\n\n        raw = await r.get(khoa)          # 1. đọc để KIỂM CHỦ, chưa đốt\n        if raw is None:            raise DeNghiHetHan\n        if chu(raw) != actor_id:   raise DeNghiCuaNguoiKhac      # ← KHÔNG đốt gì cả\n        raw2 = await r.getdel(khoa)      # 2. ĐỐT — đây là CỬA\n        if raw2 is None:           raise DeNghiHetHan\n        return DeNghiThuHoi(**json.loads(raw2))   # ← DÙNG raw2, TUYỆT ĐỐI KHÔNG raw\n\n    Hai ràng buộc, cả hai được cả hai người phản biện nêu độc lập:\n\n    · Kiểm chủ TRƯỚC khi đốt (codes.py:607-616): bấm nhầm đề nghị của đồng nghiệp không\n      được đốt đề nghị của người ta.\n    · Giá trị trả về là của `GETDEL`, không phải của `GET`. Câu "GET → so → GETDEL" mời\n      gọi viết thành "dùng lại kết quả GET"; viết sai thì hai request song song CÙNG đi\n      tiếp. Tiền không mất (AND status='available' ở code_issuance.py:442), nhưng sinh\n      HAI dòng audit cho MỘT quyết định — sổ nói dối theo hướng phóng đại.\n    """\n\n\nasync def thu_hoi_hang_loat(db: AsyncSession, de_nghi: DeNghiThuHoi, *,\n                            ip: str | None = None) -> KetQuaThuHoiHangLoat:\n    """Ghi. Gọi trong `transaction()`. NHẬN ĐÚNG MỘT THỨ: một vé đã tiêu.\n\n    `actor_id`, `so_ma_da_hien`, `tong_vnd_da_hien`, phạm vi — TẤT CẢ đọc từ `de_nghi`.\n    Không tham số nào khác ngoài `ip`. Đây là N1: không đường vào nào chạm `revoke_bulk()`\n    mà không đốt một vé, và không nơi gọi nào bịa được con số "người ta đã nhìn thấy".\n\n    NĂM việc, trong CÙNG giao dịch với UPDATE, đúng thứ tự:\n\n    1. `require_permission(db, de_nghi.actor_id, "/del_all_code")`         → PermissionDenied\n    2. `rows = await revoke_bulk(db, code_type=…, value_vnd=…)`\n       `so_ma, tong_vnd = len(rows), sum(r.value_vnd for r in rows)`\n    3. TRẦN TIỀN, đo trên CHÍNH tập vừa UPDATE:\n           threshold = get_int(DUAL_APPROVAL_KEY, DEFAULT_DUAL_APPROVAL_VND, db=db)\n           if tong_vnd > threshold: raise VuotNguongThuHoi(...)\n       ── vá B.6 / TIEN.3. Bản cũ ĐẾM rồi mới UPDATE: hai câu lệnh, hai snapshot ở READ\n       COMMITTED, và giữa chúng còn có một lượt `settings_service` có thể là truy vấn\n       thật khi cache 60 giây hết hạn. Bên kia khe là `POST /kho/nap` → `stock.nap()`\n       commit trong `transaction()` (routes/kho.py:199-200) — cùng màn hình, admin khác.\n       Đo trên `RETURNING` là phép đo CHÍNH XÁC, không phải xấp xỉ; và nó BỎ ĐƯỢC HẲN\n       câu đếm thứ ba. Đặt trần TRƯỚC kiểm lệch để kịch bản 17.605.000đ rơi vào ĐÚNG\n       chuỗi chữ đang chạy (codes.py:662-669).\n    4. KIỂM LỆCH TĂNG — hàng rào trung tâm của bản này:\n           if so_ma > de_nghi.so_ma_da_hien or tong_vnd > de_nghi.tong_vnd_da_hien:\n               raise PhamViDaLonLen(...)\n       Lệch GIẢM được dung thứ (có người vừa nhận mã) — giữ đúng codes.py:695-702.\n       Lệch TĂNG là đường tấn công.\n       ── vá B.1: đề nghị lúc kho 1 mã 5.000đ → admin khác nạp 199 mã 5k (995.000đ, qua\n          được `kiem_lo_nap` vì ≤ trần) → bấm XOÁ NGAY: `1.000.000 > 1.000.000` là False\n          nên trần CHO QUA, và 995.000đ mà không một con người nào duyệt bị huỷ vĩnh viễn.\n          Trần khai báo mức chấp nhận cho ĐỘ LỚN CỦA PHẠM VI; nó không khai báo gì về\n          ĐỘ LỆCH giữa cái đã duyệt và cái bị xoá. Hai đại lượng khác nhau.\n       ── vá TIEN mục 1: kho 10 mã 5.000đ (hiện "10 mã · 50.000đ") → 10 người nhận hết →\n          admin khác nạp 10 mã 88k → xác nhận: SỐ MÃ BẰNG NHAU (10 = 10) nên cảnh báo\n          lệch của bản cũ ĐỨNG IM, và 880.000đ bốc hơi trong im lặng. Đây là lý do vé\n          phải mang `tong_vnd_da_hien`.\n       Không có đường hoàn tác: đã grep toàn `src/`, `SET status = 'available'` chỉ xuất\n       hiện một chỗ — `reap_reservations`, code_issuance.py:370, chỉ nhận `'reserved'`\n       (`:376`).\n    5. `write_audit()` CHỈ khi `rows` khác rỗng — giữ codes.py:684 và test_admin_codes.py:488.\n\n    Ngoại lệ ở bước 1, 3, 4 ném SAU khi UPDATE đã chạy ⇒ `transaction()` cuộn lại\n    (db/engine.py:83) ⇒ 0 mã `revoked`, 0 dòng audit. ĐIỀU KIỆN BẮT BUỘC ở nơi gọi: xem B.3.\n    """\n```\n\n### A.5 SQL đi theo service\n\nChuyển vào `code_issuance.py`: `_SQL_COUNT_AVAILABLE` (`codes.py:392-399`, thêm cột `max(code_id)` **không** dùng ở bản này — bỏ, xem F), `_SQL_FIND_CODE` (`codes.py:725-740`, **thêm `c.code_value`**), `_ticket_key`/`_issue_ticket`/`_claim_ticket` (`:402-440`), `_du_nguong` (`:458-465`).\n\nMột câu mới:\n\n```sql\n-- _SQL_TONG_DA_THU_HOI: tiền một người đã thu hồi bằng /del_code trong cửa sổ trượt.\n-- Chạy được HÔM NAY, không đổi hợp đồng sổ: `entity_id = str(code_id)` (codes.py:807)\n-- và `codes` giữ nguyên hàng sau khi `revoked` (revoke_one KHÔNG delete, :454-455).\n-- Lọc `entity_id ~ '^[0-9]+$'` trong CTE để phép ép kiểu không bao giờ chạy trên dòng\n-- của action khác, bất kể planner chọn thứ tự nào.\nWITH da_thu AS (\n    SELECT entity_id\n      FROM audit_log\n     WHERE action = 'del_code'\n       AND actor_id = :uid\n       AND created_at > now() - make_interval(secs => :cua_so)\n       AND entity_id ~ '^[0-9]+$'\n)\nSELECT coalesce(sum(c.value_vnd), 0)::bigint\n  FROM da_thu d JOIN codes c ON c.code_id = d.entity_id::bigint\n```\n\n> **Bất biến mới, mang tải:** `audit_log.action='del_code'` ⇒ `entity_id = str(codes.code_id)`, và hàng `codes` không bị xoá. Trước bản này đó là một quy ước; từ bản này nó là **nguồn dữ liệu của một hàng rào tiền**. Đột biến E-07 đo nó.\n\n### A.6 Ở lại tầng trình bày\n\n`_moment()` (`codes.py:101-109`), `texts.display_name`, `DEL_USAGE` (`:123`), `DEL_ALL_USAGE` (`:378-390`), `_del_all_keyboard()` (`:443-455`), `_CB_DEL_ALL_RE` (`:369`), `handle_del_all_callback` (`:572-584`), `_args`, `_chat_id`; bên web là `NHAN_LOAI` (`routes/kho.py:49-55`). Đúng luật `stock.py:9-10` ("cách nói ở lại, cách ĐẾM chuyển đi").\n\n---\n\n## B. DANH SÁCH ĐƯỜNG WEB\n\nTất cả trong **`src/televip/apps/adminweb/routes/kho.py`** — cùng màn kho, **không mục menu mới** (`menu.py:46` đã có `/kho`, gác `/tonkho`).\n\n| Method + Path | Gác | Mã trả | Form gửi lên |\n|---|---|---|---|\n| `GET /kho` | `can_quyen("/tonkho")` | 200 | — |\n| `POST /kho/thuhoi-ma` | `Depends(kiem_csrf)` + `can_quyen("/tonkho")` + `can_quyen("/del_code")` | **200** trang kho + `xong`/`loi` + `giu` | `_csrf`, `ma` |\n| `POST /kho/thuhoi/denghi` | `Depends(kiem_csrf)` + `can_quyen("/tonkho")` + `can_quyen("/del_all_code")` | **200** `kho_thuhoi_xacnhan.html`, hoặc 200 trang kho + `loi` | `_csrf`, `loai`, `menh_gia` |\n| `POST /kho/thuhoi/xacnhan` | `Depends(kiem_csrf)` + `can_quyen("/tonkho")` + `can_quyen("/del_all_code")` | **200** trang kho + `xong` (số THẬT) / `loi` | `_csrf`, `ve` — **và không gì khác** |\n\n### B.1 Vì sao gác THÊM `/tonkho` trên ba đường ghi\n\nThân phản hồi của cả ba là `_man_hinh()` — tức toàn bộ bảng tồn kho, màn vốn gác bằng `/tonkho`. Hôm nay vô hại (mọi vai có `/del_code` đều có `/tonkho`), nhưng route phải khai **đúng cái nó trả về**. Hai `Depends`, mỗi cái vẫn là một tên lệnh bot — không đẻ ra không gian tên quyền thứ hai (`deps.py:10-14`).\n`POST /kho/nap` (`kho.py:147-155`) có **cùng ghép nối này và không khai** — pre-existing, **không sửa** (CLAUDE.md §3). Ghi nhận ở F.\n\n### B.2 Cố ý KHÔNG có `GET /kho/thuhoi`\n\nTrang xác nhận chỉ sinh ra từ một POST. Việc **phát vé** không bao giờ là tác dụng phụ của một GET (GET không qua `kiem_csrf`, `deps.py:139-185`).\n**Nhưng phải nói thẳng, vì thiết kế cũ nói sai** (B.7a): bấm Back **có** quay về trang xác nhận (bfcache giữ nguyên `ve` trong DOM, hoặc "Confirm Form Resubmission" → POST lại bước đề nghị), và trang lưu được bằng Ctrl+S hay "Copy as cURL". "Không có URL riêng" là một **tiện nghi**, không phải hàng rào. Xem C.\n\n### B.3 Ba luật cứng của route\n\n1. **`try/except ThuHoiBiTuChoi` PHẢI nằm NGOÀI `async with transaction()`.**\n   Bắt bên trong thì giao dịch commit bình thường: mã đã `revoked`, audit không ghi, màn hình báo "TỪ CHỐI" — đúng nghĩa "sổ cái lệch với bảng codes". Khuôn hiện tại của bot bắt **bên trong** (`codes.py:761-762`), nên người viết web sẽ chép đúng cái bẫy đó. Với N2 (hàng rào ném **sau** UPDATE) bẫy này thức dậy ngay lượt đầu. Áp cho **cả bot lẫn web** (mục D).\n2. **Route không kiểm lại hàng rào nào**, chỉ dịch ngoại lệ sang tiếng Việt (khuôn `kho.py:10-19`). Bắt thêm `PermissionDenied` → `raise khong_thay()` (404), giữ luật 404.\n3. **`POST /kho/thuhoi/xacnhan` đọc phạm vi TỪ VÉ, không từ request.** Đây là bản web của luật `codes.py:410-419`. Phiên bản web của lỗ hổng 17.605.000đ chính là một hidden field `loai=event`.\n\n### B.4 404 và dấu vết của lượt bị từ chối\n\n404 cho: thiếu quyền, sai/thiếu CSRF, thiếu `Origin` — `deps.py:79-85, 121-136, 139-185` lo.\n\n**Thêm vào `can_quyen._gac` (deps.py:128-134)** — vá QUYEN mục 3:\n\n```python\nasync def _gac(request: Request,\n               nguoi: Annotated[NguoiDung, Depends(phien_hien_tai)]) -> NguoiDung:\n    async with session() as db:\n        duoc = await admin_service.can_run(db, nguoi.user_id, lenh)\n    if not duoc:\n        async with transaction() as db:\n            await admin_service.write_audit(\n                db, actor_id=nguoi.user_id, action=f"{lenh}.denied",\n                entity_type="command", entity_id=lenh,\n                after={"ip": await ip_cua(request), "duong_dan": request.url.path},\n            )\n        log.warning("adminweb_tu_choi_quyen", user_id=..., role=..., lenh=lenh)\n        raise khong_thay()\n    return nguoi\n```\n\n`services/admin.py:14-20` khai điều kiện để im lặng là chấp nhận được: *"Dấu vết của lượt từ chối không mất đi: nó nằm ở `audit_log` với hành động `<lệnh>.denied`."* Bot làm đúng vậy (`admin.py:389-403`). Web tái tạo vế **im lặng** (404) và bỏ vế **bù trừ** — chỉ một dòng structlog. Màn Nhật ký (`routes/nhatky.py:67`) đọc `audit_log`, nên cửa sổ duy nhất mà vận hành nhìn vào là **mù** với mọi lượt từ chối quyền trên web. `ketoan` (có `/tonkho`, `0003_seed_admin.py:68`) POST thử 200 lượt: 0 dòng.\nKhông có nguy cơ ngập sổ: `_gac` chỉ chạy **sau** khi `phien_hien_tai` đã đậu; máy quét ẩn danh dừng ở `deps.py:97-98`.\n**Phạm vi:** chạm mọi route web ⇒ mục 0 điểm (3), phải duyệt riêng.\n\n### B.5 Vé chết trả 200, không trả 404\n\nLuật 404 bảo vệ sự tồn tại của **đường dẫn**; một trường form hết hiệu lực là **kết quả nghiệp vụ**. Trả 404 tái tạo đúng cái hỏng tệ nhất: "đốt nhưng không nói" → admin kết luận panel hỏng → quay ra gõ tay `/del_all_code` với phạm vi rộng hơn.\n\n### B.6 Ba việc phụ trong `_man_hinh()` (`kho.py:65-141`)\n\n- `duoc_thu_hoi_mot = await admin_service.can_run(db, nguoi.user_id, "/del_code")` và `duoc_thu_hoi_loat = … "/del_all_code"` — hỏi lại mỗi request, đúng cách `duoc_nap` làm (`kho.py:83`).\n- `giu` nhận thêm khoá `ma_thu_hoi`.\n- **Nhấc CẢ `xong` LẪN `loi` ra khỏi `{% if duoc_nap %}`** — vá B.7b. Đọc `templates/kho.html` thì `loi` **có** nhánh dự phòng (`:135-138 {% elif loi %}`), `xong` **không có** (`:60-63` nằm trong `:53 {% if duoc_nap %}`). Nghĩa là người thiếu `/add_giffcode` thấy được lời từ chối nhưng **không bao giờ thấy kết quả thành công** — tức không thấy *"Đã thu hồi 200 mã"* và không thấy *"⚠️ Lúc xem thử là 1 mã"*. Cảnh báo lệch rơi đúng vào nhánh template không tồn tại. Sửa: một khối `xong`/`loi` duy nhất, đặt trên cùng thân trang, ngoài mọi `{% if %}` quyền.\n\n### B.7 Template\n\n`templates/kho_thuhoi_xacnhan.html` (mới): số mã **và tổng tiền** in to nhất trang, phạm vi, hạn 5 phút; form chỉ chứa `_csrf` + `ve`. Không JS mới (`static/kho.js:1-5` cấm để hàng rào rơi vào đó).\n`templates/kho.html`: ô mệnh giá của form đề nghị **không có `<option value="">`**; lựa chọn "toàn bộ" là `<option value="__tatca__">TẤT CẢ mệnh giá</option>`, không `selected` sẵn.\n\n---\n\n## C. CƠ CHẾ CHỐNG LẶP LẠI\n\n### C.1 Nói thẳng cái gì là hàng rào, cái gì là trang trí\n\nThiết kế cũ liệt kê "năm lớp". Bản chốt xếp lại cho đúng, vì người sau sẽ tin có năm lớp trong khi chỉ có vài lớp làm việc (B.7a):\n\n| | Cơ chế | Loại |\n|---|---|---|\n| — | Trang xác nhận không có URL riêng | **Tiện nghi.** Back + bfcache trả lại nguyên trang; "Confirm Form Resubmission" POST lại bước đề nghị. |\n| — | Form chỉ mang `ve` + `_csrf` | **Bắt buộc, nhưng không phải lớp chống lặp.** Nó chặn *đổi phạm vi*, không chặn *lặp*. |\n| **H1** | Vé **một lần** (`GETDEL` là cửa), TTL 300s, neo `actor_id` | Hàng rào |\n| **H2** | **Một đề nghị sống mỗi người** (`dac:chu:<actor_id>`, `SET…GET` + `DEL` vé cũ) | Hàng rào |\n| **H3** | **Kiểm lệch tăng + trần, đo trên tập `RETURNING`, trong cùng giao dịch** | Hàng rào |\n| **H4** | `AND status = 'available'` trong `_SQL_REVOKE_BULK` (`code_issuance.py:442`) và `_SQL_REVOKE_ONE` (`:434`) | Hàng rào cuối |\n\nVà: **bấm HUỶ cũng đốt vé**; trên web không có nhánh huỷ — không bấm gì là huỷ. Không có "cỗ máy thời gian" kiểu bấm Huỷ → Back → Xoá ngay.\n\nCon số báo về **luôn lấy từ `KetQuaThuHoiHangLoat.so_ma_da_xoa`** (tức `RETURNING`), kèm cảnh báo lệch khi `kq.lech` — **so cả số mã lẫn tổng tiền**. Không bao giờ in lại con số của trang xem thử.\n\n### C.2 Bản đồ: kịch bản của người phản biện → lớp chặn nó\n\n| Kịch bản | Chặn ở | Kết quả |\n|---|---|---|\n| F5 trên `/kho/thuhoi/xacnhan` | H1 | 200 + "Đề nghị này đã đóng — không mã nào bị đụng tới." |\n| Back → gửi lại form xác nhận | H1 | như trên |\n| Hai tab cùng bấm **một** vé (REPLAY §A) | H1 (`GETDEL` nguyên tử) + H4 | đúng một lượt làm việc; kẻ thua nhận "đã đóng" |\n| **Back → mở đề nghị thứ hai/ba, quên tab cũ** (B.2) | **H2** | vé cũ chết ngay lúc vé mới sinh |\n| **Bot + web cùng lúc** (B.2, dùng chung `dac:` và `admin_users.user_id` = Telegram id) | **H2** | cái nút Telegram trong lịch sử chat chết ngay khi mở đề nghị trên web |\n| **Kho lớn lên trong 300s, VẪN DƯỚI TRẦN** (B.1, 995.000đ mỗi 300s) | **H3** kiểm lệch tăng | TỪ CHỐI, 0 mã `revoked` |\n| **Cùng số mã, tiền tăng 17,6 lần** (TIEN 1, 880.000đ) | **H3** so `tong_vnd_da_hien` | TỪ CHỐI, 0 mã `revoked` |\n| **Khe snapshot giữa đếm và UPDATE** (B.6 / TIEN 3) | **H3** đo trên `RETURNING` | không còn khe: phép đo và phép ghi là cùng một câu lệnh |\n| **Trần bị hạ xuống trong 300s chờ** | **H3** đọc lại `settings` trong giao dịch | TỪ CHỐI |\n| Vé của admin khác | H1 (kiểm chủ **trước** khi đốt) + `can_quyen("/del_all_code")` | từ chối, **vé của chủ vẫn sống** |\n| Bấm nhầm nút của đồng nghiệp trên Telegram | `int(raw_actor) != tg_user.id` (`codes.py:609`) — lớp riêng của Telegram, chạy **trước** khi chạm Redis | giữ nguyên, kể cả log `xoa_hang_loat_bam_nut_nguoi_khac` |\n| Hai admin bấm đồng thời **cùng phạm vi** | khoá dòng Postgres + `RETURNING`; kẻ thua nhận `[]`, `if rows:` không ghi audit | không cộng dồn |\n| Dán hai mã vào `/kho/thuhoi-ma` | rào 0 + `uq_codes_value` (`db/models/codes.py:140`) | từ chối |\n| **Vòng lặp `fetch` một-mã-một-request** (B.4 / QUYEN 1, 44.000.000đ) | **trần tích luỹ trong `thu_hoi_mot()`** (A.3.2) | mã đầu qua, tổng vượt trần thì dừng — ở **cả** bot lẫn web |\n| F5 sau khi thu hồi một mã thành công | H4 + `MaDaThuHoi` | idempotent thật; không đường nào đưa `revoked` về `available` |\n| **Route gọi `kiem_pham_vi_thu_hoi()` rồi gọi thẳng hàm ghi** (B.3) | **N1**: hàm ghi chỉ nhận `DeNghiThuHoi` | không biên dịch qua được — không có đường tắt để một dòng sửa hay một nút "thu hồi nhanh" mở lại |\n| **Nơi gọi bịa `so_ma_da_hien=len(rows)`** (B.3) | **N1**: tham số đó không còn tồn tại | cảnh báo lệch và `audit_log.before` không giả được |\n\n---\n\n## D. BOT SỬA GÌ\n\nMột tệp: `src/televip/apps/worker/handlers/admin/codes.py`. **Câu chữ và hành vi người dùng thấy giữ nguyên 100%**, trừ ba chỗ mới (đều là từ chối, đều fail-closed): trần tích luỹ `/del_code`, `PhamViDaLonLen`, và mệnh giá rỗng.\n\n**Xoá** (đã chuyển vào service): `_SQL_COUNT_AVAILABLE` (392-399), `_ticket_key` (402-403), `_issue_ticket` (406-429), `_claim_ticket` (432-440), `_du_nguong` (458-465), `_SQL_FIND_CODE` (725-740), và thân cũ của `_revoke_code` (771-816).\n\n**Giữ tên bằng alias** — khuôn `codes.py:63` (`parse_value_vnd`) và `:130` (`LoadResult`):\n```python\nDEL_ALL_TICKET_TTL_SECONDS: Final = code_issuance.DE_NGHI_TTL_SECONDS   # dùng ở :558\nDEL_ALL_TICKET_PREFIX: Final      = code_issuance.DE_NGHI_PREFIX\n```\n\n**`handle_del_all_code` (468-569)** — thay thân 488→545:\n```python\nasync with session() as db:\n    try:\n        pham_vi = await code_issuance.kiem_pham_vi_thu_hoi(\n            db, code_type_tho=args[0],\n            menh_gia_tho=args[1] if len(args) > 1 else code_issuance.MENH_GIA_TAT_CA,\n        )\n    except code_issuance.LoaiKhongHopLe:        # in nguyên chuỗi 490-493 (dùng args[0] THÔ)\n    except code_issuance.MenhGiaKhongDocDuoc:   # nguyên chuỗi 502-504\n    except code_issuance.PhamViRong:            # nguyên chuỗi 517-519\n    except code_issuance.VuotNguongThuHoi as e: # nguyên chuỗi 526-532 + log 533-540\nasync with transaction() as db:\n    ticket = await code_issuance.tao_de_nghi_thu_hoi(db, actor_id=tg_user.id, pham_vi=pham_vi)\n```\nChuỗi xác nhận 546-562 và `_del_all_keyboard(tg_user.id, ticket)` giữ nguyên, đọc `pham_vi.so_ma` / `pham_vi.tong_vnd`.\n\n**`_del_all_dispatch` (587-719)**:\n- Giữ nguyên kiểm `int(raw_actor) != tg_user.id` (609) **và** log `xoa_hang_loat_bam_nut_nguoi_khac`.\n- `_claim_ticket` → `nhan_de_nghi_thu_hoi(ve=ticket, actor_id=tg_user.id)`; `except DeNghiHetHan` in nguyên chuỗi 622-628; `except DeNghiCuaNguoiKhac` in nguyên chuỗi 610-614.\n- Nhánh `no` (636-647) không đổi, đọc từ `de_nghi`.\n- **Xoá hẳn** khối đếm lại 652-657 + `_du_nguong` 658 — nó đã vào trong hàm ghi (N2).\n- Khối 681-693 thành:\n```python\ntry:\n    async with transaction() as db:                       # ← except NGOÀI transaction\n        kq = await code_issuance.thu_hoi_hang_loat(db, de_nghi)\nexcept code_issuance.VuotNguongThuHoi as e:\n    ...  nguyên chuỗi 662-669 (e.tong_vnd, e.so_ma, e.threshold_vnd,\n         de_nghi.so_ma_da_hien) + log 671-678 ...\n    return\nexcept code_issuance.PhamViDaLonLen as e:\n    ...  chuỗi MỚI, BẮT BUỘC mở đầu "⛔ TỪ CHỐI — không mã nào bị đụng tới." ...\n    return\n```\n- Cảnh báo lệch 698-702 và chuỗi kết 703-711 giữ nguyên, đọc `kq.so_ma_da_xoa` / `kq.tong_vnd` / `kq.lech`.\n\n**`handle_del_code` (743-768)** — `_revoke_code` bị xoá (không bài kiểm nào tham chiếu nó; đã grep toàn `tests/`). Thân mới:\n```python\ntry:\n    async with transaction() as db:                       # ← except NGOÀI transaction\n        ma = await code_issuance.kiem_ma_thu_hoi(db, code_value=code_value)\n        code_id = await code_issuance.thu_hoi_mot(db, ma, actor_id=tg_user.id)\nexcept code_issuance.KhongTimThayMa:  reply = f"❌ Không tìm thấy code: {code_value}"\nexcept code_issuance.MaDaPhat as e:   reply = ( ...nguyên văn 786-790, dùng e.holder_*,\n                                                 _moment(e.moc)... )\nexcept code_issuance.MaDaThuHoi:      reply = f"⚠️ Code {code_value} đã bị thu hồi trước đó."\nexcept code_issuance.MatCuocDua:      reply = (f"❌ Code {code_value} vừa được một luồng "\n                                               f"khác giữ chỗ. Không thu hồi được.")\nexcept code_issuance.VuotNguongThuHoiTichLuy as e:   reply = ...chuỗi MỚI...\nexcept code_issuance.ThuHoiBiTuChoi as e:            reply = f"❌ {e}"   # gồm NhieuHonMotMa\nelse:\n    reply = ( ...nguyên văn 812-815, dùng ma.code_value / ma.code_type / ma.value_vnd... )\n    log.info("thu_hoi_code", actor_id=..., code_id=code_id, code_value=ma.code_value)\nawait sender.send_message(chat_id, reply)\n```\n\n**Dọn import mồ côi do chính thay đổi này sinh ra**: bỏ `json` (32), `secrets` (34), `from televip.cache.client import get_redis` (44). Giữ `text`, `session`, `transaction`, `AsyncSession`, `settings_service`, `write_audit` (còn dùng ở 902, 922).\n\n**Hai dòng audit của bot giữ nguyên và cố ý khác web**: decorator vẫn ghi `action="/del_code"` / `"/del_all_code"`, `entity_type="command"` (`admin.py:405-414`); web không có dòng tương đương. Bài kiểm chỉ đếm dòng không có dấu `/`.\n\n---\n\n## E. DANH SÁCH ĐỘT BIẾN\n\nBài kiểm mới: **`tests/test_adminweb_thuhoi.py`** (khuôn `tests/test_adminweb_kho.py`). **`tests/test_admin_codes.py` không được sửa một dòng** — đó là phép đo "hành vi bot giữ nguyên". Đã đối chiếu từng bài: bốn bài `_de_nghi` dựng kho **trước** khi đề nghị nên `so_ma`/`tong_vnd` không đổi → xanh; `:759-773` đốt vé ở lượt một nên không tới bước kiểm → xanh; `:775-798` gặp trần **trước** kiểm lệch nên rơi vào đúng chuỗi 662-669 chứa "TỪ CHỐI" → xanh; `:800-816` bị chặn ở lớp `callback_data` nên không chạm Redis → xanh; `:494-510` chỉ tiêu 10.000đ nên trần tích luỹ (mặc định 1.000.000đ) không cắn → xanh.\n\nKý hiệu bài kiểm mới: **W**n. Mỗi dòng: *đổi gì* → *bài nào phải ĐỎ*.\n\n| # | Gỡ / đổi (dòng cụ thể) | Bài kiểm phải ĐỎ |\n|---|---|---|\n| E-01 | `kiem_ma_thu_hoi`: xoá `if len(token) != 1: raise NhieuHonMotMa` | **W3** `test_dan_hai_ma_bi_tu_choi` |\n| E-02 | `kiem_ma_thu_hoi`: truyền `code_value` thô vào `_SQL_FIND_CODE` thay vì `token[0]` | **W4** `test_khoang_trang_thua_van_thu_hoi_dung_ma` (gửi `"  ABC-1  \r\n"`) |\n| E-03 | Bỏ `c.code_value` khỏi `_SQL_FIND_CODE`; `MaChoThuHoi.code_value = code_value` (tham số) | **W5** `test_so_ghi_code_value_tu_DATABASE` (`after->>'code_value'` phải **không** chứa `\r\n`) |\n| E-04 | `kiem_ma_thu_hoi`: bỏ rào `grant_id IS NOT NULL` | **W2** `test_ma_da_phat_bi_tu_choi_va_khong_ghi_so` **và** `test_admin_codes.py::test_del_code_tu_choi_ma_da_phat_va_khong_doi_trang_thai` |\n| E-05 | `_SQL_REVOKE_ONE` (`code_issuance.py:434`): bỏ `AND status = 'available'` | **W23** `test_ma_dang_giu_cho_khong_thu_hoi_duoc` (mã `reserved` → 200 + từ chối, `status` vẫn `reserved`) |\n| E-06 | `thu_hoi_mot`: bỏ `require_permission(db, actor_id, "/del_code")` | **W22** `test_hang_rao_quyen_nam_trong_HAM_GHI` (gọi thẳng service với actor thiếu quyền → `PermissionDenied`, 0 mã `revoked`) |\n| E-07 | `thu_hoi_mot`: `entity_id=str(ma.code_id)` → `entity_id=None` | **W1** `test_thuhoi_mot_ma_va_ghi_so_co_ip` **và** **W6** (mất nguồn dữ liệu của trần tích luỹ) |\n| E-08 | `thu_hoi_mot`: bỏ khối trần tích luỹ | **W6** `test_tran_tich_luy_chan_vong_lap_thu_hoi_tung_ma` (trần 50.000đ · 6 lượt × 10.000đ; lượt 6 phải 200-từ-chối và `count(revoked) == 5`) |\n| E-09 | `thu_hoi_mot`: ghi `write_audit` cả khi `revoke_one()` trả `None` | **W23** (từ chối mà vẫn có dòng sổ) |\n| E-10 | `tao_de_nghi_thu_hoi`: bỏ `require_permission(…, "/del_all_code")` | **W21b** `test_hang_rao_quyen_hang_loat_nam_trong_service` |\n| E-11 | `tao_de_nghi_thu_hoi`: bỏ `write_audit(action="del_all_code.denghi")` | **W10** `test_denghi_khong_cham_codes_nhung_co_dong_audit_denghi` |\n| E-12 | `tao_de_nghi_thu_hoi`: bỏ `SET dac:chu:<actor>` + `DEL` vé cũ | **W12** `test_moi_nguoi_toi_da_MOT_de_nghi_song` (mở hai đề nghị, tiêu cái mới, cái cũ phải chết) |\n| E-13 | `tao_de_nghi_thu_hoi`: bỏ `tong_vnd` khỏi payload vé (quay về thiết kế cũ) | **W15** `test_cung_so_ma_nhung_TIEN_tang_thi_TU_CHOI` |\n| E-14 | `nhan_de_nghi_thu_hoi`: `return DeNghiThuHoi(**json.loads(raw))` — dùng giá trị của `GET` | **W24** `test_hai_lan_tieu_cung_ve_dong_thoi_chi_MOT_lan_lam_viec` (`asyncio.gather` hai POST cùng `ve`; đúng **một** thân chứa "Đã thu hồi", đúng **một** chứa "đã đóng") |\n| E-15 | `nhan_de_nghi_thu_hoi`: bỏ kiểm `actor_id` | **W13a** `test_ve_cua_admin_khac_bi_tu_choi` |\n| E-16 | `nhan_de_nghi_thu_hoi`: kiểm `actor_id` **sau** `GETDEL` | **W13b** vế hai: chủ vé vẫn bấm được sau khi người khác thử |\n| E-17 | `thu_hoi_hang_loat`: bỏ `if so_ma > de_nghi.so_ma_da_hien` | **W14** `test_kich_ban_995_000d_duoi_tran` (đề nghị lúc 1 mã 5k → nạp 199 mã 5k → xác nhận phải TỪ CHỐI, `revoked == 0`) |\n| E-18 | `thu_hoi_hang_loat`: bỏ `or tong_vnd > de_nghi.tong_vnd_da_hien` | **W15** (10 mã 5k đã hiện → 10 mã 88k thật; số mã bằng nhau) |\n| E-19 | `thu_hoi_hang_loat`: bỏ kiểm trần `tong_vnd > threshold` | **W25** `test_tran_ha_xuong_giua_hai_buoc_thi_TU_CHOI` (đề nghị 500.000đ dưới trần 1.000.000 → hạ trần xuống 100.000 → xác nhận phải TỪ CHỐI, `revoked == 0`) |\n| E-20 | `thu_hoi_hang_loat`: đổi chữ ký về `(db, pham_vi: PhamViThuHoi, *, actor_id, so_ma_da_hien)` | **W20** `test_ham_ghi_hang_loat_chi_nhan_DeNghiThuHoi` (`inspect.signature`: tham số vị trí thứ hai chú thích `"DeNghiThuHoi"`, và **không có** tham số `so_ma_da_hien`) |\n| E-21 | `thu_hoi_hang_loat`: bỏ `if rows:` quanh `write_audit` | **W26** `test_pham_vi_can_kho_thi_khong_ghi_so` (đề nghị xong, mọi mã bị `issued` bên ngoài → 0 mã, **0** dòng `del_all_code`, có cảnh báo lệch) |\n| E-22 | `thu_hoi_hang_loat`: báo `de_nghi.so_ma_da_hien` thay vì `len(rows)` ở chuỗi kết | **W16** `test_in_so_tu_RETURNING_kem_canh_bao_lech` (kho vơi đi giữa hai bước) |\n| E-23 | `_SQL_REVOKE_BULK` (`:442`): bỏ `AND status = 'available'` | **W27** `test_hang_loat_khong_dung_toi_ma_da_phat` **và** `test_admin_codes.py::test_del_all_code_khong_dung_toi_ma_da_phat` |\n| E-24 | Route `xacnhan`: đưa `try/except` **vào trong** `async with transaction()` | **W14** (màn hình báo TỪ CHỐI nhưng `count(revoked) != 0`) |\n| E-25 | Route `xacnhan`: đọc `loai`/`menh_gia` từ `Form()` thay vì từ vé | **W28** `test_form_xac_nhan_bo_qua_moi_truong_ngoai_ve` (gửi kèm `loai=event&menh_gia=__tatca__` khác phạm vi vé) |\n| E-26 | `kho_thuhoi_xacnhan.html`: thêm `<input type="hidden" name="loai">` | **W18** `test_trang_xac_nhan_chi_co_hai_truong` (đếm `name=` trong `<form>` = đúng 2) |\n| E-27 | Route `thuhoi-ma`: bỏ `Depends(kiem_csrf)` | **W8** `test_thieu_csrf_va_thieu_Origin_deu_404` |\n| E-28 | Route hàng loạt: đổi `can_quyen("/del_all_code")` → `can_quyen("/del_code")` | **W21a** `test_co_del_code_nhung_khong_co_del_all_code_thi_404` |\n| E-29 | Route `thuhoi-ma`: bỏ `can_quyen("/del_code")`, chỉ để `/tonkho` | **W7** `test_khong_co_quyen_del_code_thi_404_va_trang_khong_hien_form` |\n| E-30 | `deps.can_quyen._gac`: bỏ `write_audit(f"{lenh}.denied")` | **W9** `test_tu_choi_quyen_web_de_lai_dong_denied` |\n| E-31 | `kiem_pham_vi_thu_hoi`: coi `""` như `MENH_GIA_TAT_CA` | **W17** `test_menh_gia_rong_KHONG_duoc_hieu_la_TAT_CA` |\n| E-32 | `kho.html`: đưa khối `xong` trở lại trong `{% if duoc_nap %}` | **W19** `test_thong_bao_xong_hien_ca_khi_khong_co_quyen_nap` |\n| E-33 | Dán `_SQL_REVOKE_ONE` trở lại `routes/kho.py` | `scripts/check_architecture.py` **thoát 1** (luật 3, `:144-167`) |\n| E-34 | `_man_hinh`: nhớ `duoc_thu_hoi_mot` vào phiên thay vì hỏi lại mỗi request | **W29** `test_thu_hoi_quyen_giua_hai_luot_bam_thi_404_ngay` |\n\nChạy nghiệm thu:\n```\nPYTHONPATH=src .venv/Scripts/python.exe scripts/check_architecture.py\n.venv/Scripts/python.exe -m pytest tests/test_admin_codes.py tests/test_adminweb_kho.py \\n    tests/test_adminweb_thuhoi.py tests/test_code_issuance.py tests/test_adminweb.py -q\n```\n\n---\n\n## F. CỐ Ý KHÔNG LÀM\n\n| Không làm | Lý do |\n|---|---|\n| **Neo `AND code_id <= :moc_code_id` vào `_SQL_REVOKE_BULK`** | Đây là bản **tốt hơn** của H3 và tôi công nhận: nó là vị từ nằm **trong chính câu UPDATE** (lý lẽ B.6), nó **bao dung hơn** bản đang chốt (giữ được ca lành "kho lớn lên nhưng tôi vẫn muốn xoá đúng 1 mã đã thấy"), nó chặn cả khe `reap_reservations` đưa `reserved → available` (`code_issuance.py:370`) lọt vào phạm vi, và nó chặn cửa sổ khoá của một `UPDATE` bị cuộn lại. Lý do hoãn **không** phải hai lý do cũ ("đổi SQL tiền", "lệch bot khỏi web") — cả hai gỡ được. Lý do thật: nó làm **`tests/test_admin_codes.py:775-798` ĐỎ**. Với neo, bấm nút cũ sẽ thu hồi đúng 1 mã 5.000đ thay vì "TỪ CHỐI, 0 mã". Đó là **đổi hành vi bot**, và bài kiểm đó chính là phép đo "hành vi bot giữ nguyên" của đợt này. Đề xuất riêng, đợt sau, áp cho **cả hai** đường vào, kèm sửa bài kiểm đó một cách có ý thức. |\n| **Thu hồi mã ĐÃ PHÁT** (bút toán ngược `code_ledger.direction = -1`) | Nghiệp vụ chưa tồn tại trong repo: `direction = -1` chỉ ra hai *comment* (`codes.py:782`, `db/models/codes.py:349`); `ref_entry_id` (`models/codes.py:390-394`) chưa có mã nào ghi vào. Đây là xây mới, không phải "đưa lên web". Fail-closed: `MaDaPhat`. |\n| **Dán nhiều mã ở màn `/del_code` web** | Ai cần nhiều thì đi màn hàng loạt (có vé, có trần, có kiểm lệch). Lưu ý: sau khi thêm trần tích luỹ (A.3.2), câu *"web nhận đúng một mã"* mới **thật sự** mua được cái gì — trước đó vòng lặp `fetch` khiến lệnh cấm dán-nhiều-mã gần như vô nghĩa. |\n| **Thêm `code_type`/`value_vnd` vào `audit_log.after` của `del_code`** | Đổi hợp đồng sổ đang chạy (CLAUDE.md §3). Và QUYEN đúng khi nói thiết kế cũ **nói quá**: `entity_id = str(code_id)` cho phép join `codes` ra `value_vnd`, nên đây là **thiếu tiện, không thiếu bằng chứng**. Từ bản này chính phép join đó là nguồn của một hàng rào tiền (A.5), nên nó được đo bằng E-07. |\n| **Bảng `recall_drafts`** | Migration + một vòng đời không job nào tiêu thụ. Nhét vào `broadcast_jobs` thì `kind` đang là hàng rào phân quyền (`broadcast.py`) — thêm `kind='recall'` là mở đúng cửa nó đang đóng. |\n| **Đổi chữ ký `revoke_one` / `revoke_bulk`** | Nguyên thuỷ SQL, giữ nguyên; lớp nghiệp vụ bọc bên ngoài. |\n| **`LIMIT` / phân lô cho `revoke_bulk`** | Không limit, không `statement_timeout`, pool 10 chỗ `max_overflow=0` (`core/config.py:53`, `db/engine.py:36`). Bản này **làm nợ đó rõ hơn một chút**: với N2, một lượt bị từ chối vẫn `UPDATE` rồi cuộn lại, tức vẫn giữ khoá trên N dòng trong khoảnh khắc đó. Thứ giữ N nhỏ vẫn là trần tiền, và đợt này vừa đưa trần vào trong hàm ghi. Ghi nhận là **nợ kỹ thuật đã biết**; neo `code_id <= :moc` ở dòng đầu bảng này là cách trả nó. |\n| **Luồng ký thứ hai thật** | Chưa xây; `/add_giffcode` và `/del_all_code` đang fail-closed và nói ra lối thoát. Web nói y hệt. |\n| **Mục menu mới, tên quyền mới** | `/kho` đã có (`menu.py:46`). Quyền dùng đúng tên lệnh bot: `/del_code` = `("owner","admin")`, `/del_all_code` = `("owner",)` (`0003_seed_admin.py:53-54`). |\n| **Ghi audit cho lượt bị từ chối NGHIỆP VỤ trên web** (mã đã phát, vượt trần, vé chết) | Giữ hợp đồng sổ giống hệt bot (`codes.py:684`, `test_admin_codes.py:488`). Chỉ vào structlog. Khác hẳn lượt bị từ chối **QUYỀN** — cái đó **có** ghi (B.4), vì `services/admin.py:14-20` đã hứa nó. |\n| **Sửa ghép nối `/tonkho` của `POST /kho/nap`** | Pre-existing (`kho.py:147-155` trả `_man_hinh()` mà chỉ gác `/add_giffcode`). Ba đường mới khai đủ hai quyền; đường cũ không đụng (CLAUDE.md §3). **Nhắc migration sau: đừng tách `/del_code`/`/add_giffcode` khỏi `/tonkho` mà không sửa `/kho/nap` cùng lúc.** |\n| **Ghi chú phân biệt `/del_code` ↔ `del_code` trên `nhatky.html`** | Bẫy có thật: ô lọc so `action` **bằng nhau tuyệt đối** (`admin.py:502`), nên lọc `/del_code` (cái tên người ta biết, cái tên có trong `/help_admin`) trả **0 lượt từ panel** — trông y hệt câu trả lời "không có gì". Sửa đúng là một `<p>` trên ô lọc của màn Nhật ký: `action` có dấu `/` = lượt gọi lệnh trên Telegram, không có dấu `/` = kết quả nghiệp vụ, mọi đường vào. Ngoài phạm vi màn kho → xin duyệt riêng, một dòng template. Không thêm dòng audit thứ hai (sẽ đếm trùng và làm đỏ `test_admin_codes.py:488`). |\n| **Thêm khoá `"nguon": "web"` vào `after`** | Mục 5 cược vào `ip` để phân biệt nguồn, mà `ip_cua` trả `None` khi `request.client is None` (`deps.py:58-60`). Đổi hợp đồng sổ, cùng loại với dòng trên — xin duyệt cùng lượt nếu muốn chắc chắn hơn `ip`. |\n| **Trigger `grants_immutable()`** | `db/models/codes.py:212` khai `code_grants.code_id` bất biến một chiều *"trigger `grants_immutable()`"* — **trigger đó không có trong migrations**. Không ảnh hưởng thiết kế này, nhưng comment đang hứa một hàng rào không tồn tại. Báo cáo, **không sửa** (ngoài phạm vi). |\n\n---\n\n## Phụ lục — đối chiếu từng phát hiện của ba người phản biện\n\n| Phát hiện | Xử lý ở bản chốt |\n|---|---|\n| **B.1** kho lớn lên dưới trần (995.000đ/300s) | Vá — `PhamViDaLonLen`, A.4 bước 4. Đo bởi **W14**/E-17. Bài nghiệm thu số 9 của thiết kế cũ (*"kho đổi giữa hai bước dưới trần → vẫn thu hồi"*) **bị gỡ bỏ khỏi điều kiện nghiệm thu**: nó mã hoá lỗ hổng thành hành vi mong đợi. Thay bằng **W16** (chỉ dung thứ lệch **giảm**). |\n| **B.2** vé sống chồng chất | Vá — N3, `dac:chu:<actor_id>`. **W12**/E-12. |\n| **B.3** kiểu không chứng minh vé đã đốt; `so_ma_da_hien` tự do | Vá — N1, `thu_hoi_hang_loat(db, de_nghi)`. **W20**/E-20. |\n| **B.4 / QUYEN 1** `/del_code` không trần tích luỹ (44.000.000đ) | Vá — trần tích luỹ trong `thu_hoi_mot()`, SQL trên `audit_log ⋈ codes`, không Redis (Redis sẽ làm đỏ `test_admin_codes.py` vì fixture `owner` không có Redis). **W6**/E-08. **Cần duyệt** (đổi hành vi bot). |\n| **B.5** bước đề nghị web không vào sổ | Vá — `del_all_code.denghi` trong `tao_de_nghi_thu_hoi`. **W10**/E-11. |\n| **B.6 / TIEN 3** trần lượt ba là hai snapshot | Vá — N2, đo trên `RETURNING`, bỏ hẳn câu đếm thứ ba. Kèm luật "except NGOÀI transaction" (B.3, D). **W25**/E-19, **W14**/E-24. |\n| **B.7a** năm lớp là ba lớp | Vá — C.1 xếp lại thành H1–H4 và gọi hai lớp còn lại là tiện nghi. |\n| **B.7b** `xong` không có nhánh ngoài `duoc_nap` | Vá — B.6, một khối duy nhất ngoài mọi `{% if %}` quyền. **W19**/E-32. |\n| **TIEN 1** cảnh báo canh SỐ MÃ, thứ mất là TIỀN (880.000đ im lặng) | Vá — vé mang `tong_vnd_da_hien`; kiểm lệch **và** cảnh báo so cả hai; `audit_log.before` ghi cả hai. **W15**/E-13, E-18. |\n| **TIEN 2** không có bất biến "một đề nghị sống" | Vá — như B.2. Kèm ràng buộc "dùng giá trị của GETDEL" thành **W24**/E-14. |\n| **TIEN 4** `_SQL_FIND_CODE` không SELECT `code_value` | Vá — thêm cột; tra bằng `token[0]`; `MaChoThuHoi.code_value` từ dòng DB. **W4**/E-02, **W5**/E-03. |\n| **TIEN 5** `menh_gia` rỗng = TẤT CẢ | Vá — `MENH_GIA_TAT_CA` tường minh ở service (dùng chung bot+web), `<select>` không có option rỗng. **W17**/E-31. |\n| **TIEN** trigger `grants_immutable()` không tồn tại | Báo cáo, không sửa (F). |\n| **QUYEN 2** trần tiền vào điểm nghẽn, QUYỀN thì không | Vá — `require_permission` trong `thu_hoi_mot`, `tao_de_nghi_thu_hoi`, `thu_hoi_hang_loat`. **W22**/E-06, **W21b**/E-10. |\n| **QUYEN 3** từ chối quyền trên web không để lại dấu vết | Vá — `.denied` trong `deps.can_quyen._gac`. **W9**/E-30. **Cần duyệt** (chạm mọi route). |\n| **QUYEN** ô lọc Nhật ký `/del_code` ↔ `del_code` | Không làm đợt này, ghi ở F kèm bản sửa chính xác. |\n| **QUYEN** ghép nối `/tonkho` trên đường ghi | Ba đường mới khai đủ hai quyền (B.1); `/kho/nap` giữ nguyên, ghi ở F. |\n| REPLAY §A (7 đường sạch), QUYEN §"đã thử phá" (5 mục) | Giữ nguyên toàn bộ cơ chế đã đứng được: `GETDEL` nguyên tử, `AND status='available'` ở cả hai câu, kiểm `raw_actor` ở lớp `callback_data` (`codes.py:609`) + log, `khong_thay()` 404 (`deps.py:79-85`), cache vai trò fail-closed (`admin.py:178-179`), vé chết trả 200. |
